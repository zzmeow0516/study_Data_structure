/*
2022/3/26
1.线性表：0 个或者 n 个 相同数据类型 的数据元素的有限序列
    数据元素可以有多个数据项，例如数据元素：学生，可以有姓名性别...
2.线性表的ADT（抽象数据类型）

2022/4/6
1.接3/26线性表的ADT
   1.1 传参原则：当需要传递参数给函数的时候，参数的形式取决于在函数体中参数是否会发生变化。如果会，则传递/返回指向参数的指针；如果不会改变，则可以直接传递/返回参数。
   1.2 ADT中的Operation:InitList(*L),ListEmpty(L),ClearList(*L),GetElem(L,i,*e),LocateElem(L,e),ListInsert(*L,i,e),ListDelete(*L,i,e),ListLength(L).
*/
#include
